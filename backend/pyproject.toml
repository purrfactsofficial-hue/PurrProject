[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.coverage.run]
omit = [
  "venv/*",
  "tests/*",
  "scheduler.py",
  "routes/schedule.py",
  "routes/analytics.py",
]

[tool.coverage.report]
show_missing = true
